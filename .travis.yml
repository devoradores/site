language: generic
deploy:
  provider: s3
  access_key_id: $AWS_ACCESS_KEY_ID
  secret_access_key: $AWS_SECRET_ACCESS_KEY
  bucket: devoradores.com.br
  local-dir: src
  acl: public_read
  on:
    repo: devoradores/site
